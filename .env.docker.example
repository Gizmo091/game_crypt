# Configuration Docker pour Coded Words

# URL du dépôt Git à cloner (défaut: repo officiel)
REPO_URL=https://github.com/Gizmo091/game_crypt.git

# Branche à utiliser (défaut: main)
BRANCH=main

# === Configuration Serveur ===

# Port externe du serveur (défaut: 3001)
SERVER_PORT=3001

# Origines CORS autorisées (séparées par des virgules)
# Doit correspondre à l'URL du client
CORS_ORIGINS=http://localhost,https://game.example.com

# URL GitHub pour la mise à jour des phrases (optionnel)
# GITHUB_REPO_URL=https://github.com/Gizmo091/game_crypt

# === Configuration Client ===

# Port externe du client (défaut: 80)
CLIENT_PORT=80

# URL du serveur Socket.io (vue par le navigateur)
# Doit pointer vers le serveur accessible depuis l'extérieur
VITE_SERVER_URL=http://localhost:3001
